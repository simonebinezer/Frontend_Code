pipeline {
    agent any
    stages {
        stage('Verify zip') {
            steps {
                sh 'which zip || echo "zip not found"'
                sh 'zip -v || echo "zip command failed"'
            }
        }
    }
}
